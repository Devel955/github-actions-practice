name: Scheduled Job

on:
  schedule:
    - cron: '0 0 * * *'   # Roz midnight UTC

jobs:
  scheduled-run:
    runs-on: ubuntu-latest
    steps:
      - name: Scheduled run message
        run: echo "Yeh workflow roz midnight pe chalta hai!"
```

**📝 Notes ke liye answer:**
```
Har Monday 9 AM UTC ka cron:
0 9 * * 1
