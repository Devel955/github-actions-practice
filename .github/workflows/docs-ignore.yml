name: Ignore Docs Only Changes

on:
  push:
    branches:
      - main
      - 'release/*'
    paths-ignore:
      - '*.md'
      - 'docs/**'

jobs:
  run-if-not-only-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Run job
        run: echo "Workflow ran because change was not only docs"
