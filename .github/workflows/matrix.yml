name: Matrix Build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Fail on Ubuntu 3.10 only
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.10'
        run: exit 1

      - name: Print Python version
        run: python --version
```

### Step 4 — Commit karo
- Message: `Remove exclude - show 6 jobs`
- **Commit to main** ✅

---

Commit hone ke baad **Actions → Matrix Build** mein **6 jobs** dikhenge:
```
Ubuntu  + 3.10 ❌ (exit 1 se fail)
Ubuntu  + 3.11 ✅
Ubuntu  + 3.12 ✅
Windows + 3.10 ✅ (ab exclude nahi!)
Windows + 3.11 ✅
Windows + 3.12 ✅
